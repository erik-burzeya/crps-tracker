import { Text, View } from 'react-native';

export default function NewEntry() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Neuer Eintrag funktioniert</Text>
    </View>
  );
}